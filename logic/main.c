#include <stdio.h>
#include <stdlib.h>
int min(inta,intb)
{
    if(a<b)
    printf("%d is the small namber",a);
    else
    printf("%d is the small namber",b);
}

int main()
{

    int a,b;
    printf("Value of the two numbers :");
    scanf("%d",&a);
    scanf("%d",&b);

    return min;
}
    return 0;
}
