#include <stdio.h>
#include <stdlib.h>

int main()
{
    float r;
    float pi=3.1416;
    printf("Radius of the Circle:");
    scanf("%f",&r);
    printf("Circumference of the Circle = %f",pi*r*r);
    return 0;
}
