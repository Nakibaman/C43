#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    int b;
    int c
    scanf("Inter the valu of a %d",&a);
    scanf("Inter the valu of b %d",&b);
    scanf("Inter the valu of c %d",&c);
    printf("Sum of three number = %d",a+b+c);
    printf("Multiplicatio of three number = %d",a*b*c);
    printf("Devision of three number = %d",a/b/c);
    printf("Operation of three number = %d",(a+b)*c);

    return 0;
}
