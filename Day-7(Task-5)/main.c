#include <stdio.h>
#include <stdlib.h>

int main()
{
    int marks;
    scanf("%d",&marks);
    if(marks>=80)
    printf("In Marit List");
    else
    printf("Is not in Marit List");
    return 0;
}
